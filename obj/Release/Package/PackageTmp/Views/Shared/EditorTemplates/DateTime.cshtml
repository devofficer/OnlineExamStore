@model DateTime?
<style>
    .input-group {
        margin-bottom: 0px;
    }
</style>
<div class="input-group">
    @*<input type="text" class="form-control date-input" id="@ViewData.TemplateInfo.HtmlFieldPrefix" placeholder="dd/mm/yyyy" />
        <label class="input-group-btn" for="@ViewData.TemplateInfo.HtmlFieldPrefix">
            <span class="btn btn-default">
                <span class="glyphicon glyphicon-calendar"></span>
            </span>
        </label>*@
    @Html.TextBox("", (Model.HasValue ? Model.Value.ToShortDateString() : DateTime.Now.ToShortDateString()), new { @class = "form-control datepicker" })
    <label class="input-group-btn" for="@ViewData.TemplateInfo.HtmlFieldPrefix">
        <span class="btn btn-default">
            <span class="glyphicon glyphicon-calendar"></span>
        </span>
    </label>
</div> 